services:
  traefik:
    extends:
      file: ./reverse-proxy/traefik.yaml
      service: traefik

  portainer:
    extends:
      file: ./dashboard/portainer.yaml
      service: portainer

  lavalink:
    extends:
      file: ./media/lavalink.yaml
      service: lavalink

networks:
  proxy:
    name: proxy
  lavalink:
    name: lavalink

volumes:
  traefik_letsencrypt:
  portainer_data:
