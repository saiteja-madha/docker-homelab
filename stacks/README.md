# Stacks

Place app-specific Docker Compose files here.

Example:

```text
stacks/
├── app1/
│   ├── docker-compose.yml
│   └── .env.example
├── app2/
└── shared/
```

Do not commit real `.env` files.
